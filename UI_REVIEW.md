# UI 重新设计评估 v2（只列不改）

> 参考风格：**Linear / Raycast / Vercel / Figma 设置页** —— 极简、克制、精确间距、精致控件。
> 截图见 `gui_window.png`。

## 一、为什么"不现代"（根因，不是缺元素）

1. **配色 = 程序员默认暗色**：大块 `#0d1117/#161b22` 纯色、无层次梯度，像 IDE 主题，不像产品。
2. **控件 = 上过色的默认 Qt 控件**：大色块按钮、原生复选框、无 hover/active 过渡，没有一个是「设计过的组件」。
3. **空**：876×639 里内容只占顶部 ~150px，下面 400px 全空——不是留白，是没排版。
4. **无信息架构**：单一 GroupBox 塞状态+复选+按钮，无标题层级、无分区、无视觉重心。
5. **无品牌**：无 logo、无主题色、图标是系统默认。

## 二、设计方向（目标风格）

- **极简**：信息尽量少而清晰，留白是有意的节奏，不是空缺。
- **克制用色**：一个主色做 CTA + 状态色反馈，其余中性灰阶。
- **精确**：统一 8px 网格（padding/间距/圆角全是 8 的倍数）。
- **组件化**：按钮/输入/开关/卡片/导航都是可复用、带状态（hover/active/disabled）的组件。

## 三、设计方案

### 3.1 配色 Token（集中定义，替换散落色值）

| Token | 值 | 用途 |
|---|---|---|
| `bg` | `#0f1115` | 窗口背景 |
| `surface` | `#161a20` | 面板/卡片底 |
| `surface-2` | `#1e232b` | 悬浮/hover |
| `border` | `#262b33` | 分隔线 |
| `text` | `#e8eaed` | 主文字 |
| `text-muted` | `#9aa3ad` | 次文字 |
| `text-faint` | `#5c656f` | 提示 |
| `accent` | `#6c8cff` | 主色（CTA、选中） |
| `accent-soft` | `#6c8cff1a` | 主色 10% 透明（选中底） |
| `success` | `#3fb950`；`warning` `#d29922`；`danger` `#f85149` | 状态 |

### 3.2 字体

- 窗口/控件 13px → **14px**；页面标题 18px 粗；副标题 12px `text-muted`。
- 统一系统 sans（Segoe UI + 微软雅黑），字重分层（600/500/400）。

### 3.3 布局（整窗）

- 窗口默认 **920×620**，内容区 `QScrollArea`/`stretch` 撑满，不空。
- 顶部一条小的**品牌栏**：应用图标 + 「AI Passport 语音输入前端」+ 右侧状态微点 + 最小化到托盘按钮。
- 左导航（`QListWidget` 自绘）：
  - 每项「图标 + 文字」，高 44px，圆角 8px，字 14px；
  - 选中项：`accent-soft` 底色 + **左侧 3px 主色指示条**（不是整块高亮）；
  - 图标用 Qt 内置 `QStyle` 或内嵌 SVG（电源/钥匙/时钟/心形）。
- 内容区每个页面顶部有「页面标题 + 副标题」。

### 3.4 组件

**按钮**
- 主按钮：`accent` 渐变、圆角 10px、padding 10×20、hover 提亮、active 微缩、disabled 灰。
- 次按钮：描边（`border` + 透明底），hover 提亮底。
- 危险按钮：红描边或红字，不整块大红色。

**输入框**
- 高 38px、圆角 8px、`surface` 底；聚焦时 `accent` 描边 + 外发光。
- 密钥框密码模式 + 显示切换。

**开关（toggle）**
- 圆角滑动开关替代原生复选框，选中 `accent`。

**卡片**
- `surface` 底 + `border` 8px 圆角 + 12px padding + 轻微阴影；内容按分组卡片组织。

**状态徽章**
- 彩色小圆点 + 文字（运行中=绿、设备连接数=N）。

### 3.5 页面具体设计

**服务控制页**
- 顶部「状态总览」卡片：大号状态徽章（绿点运行中/灰点未运行）+ 三格指标（server_id 短码、设备连接数、注入后端），三格带分隔。
- 下方「操作」卡片：主按钮「启动服务端」、次按钮「最小化到托盘」、危险按钮「退出」。
- 「开机自启动」开关控件，右侧附说明。
- 卡片撑满内容区，无大片空白。

**API 配置页**
- STT 分组卡片：引擎下拉（iflytek/siliconflow）+ 讯飞三字段 / 硅基流动一字段。
- LLM 分组卡片：DeepSeek key。
- 密钥框密码模式 + 显示切换；保存用右上角 toast 反馈。

**历史记录页**
- 顶部筛选行（全部/仅输入/仅修改 + 时间范围）。
- 两个 tab 表格：行高 44、表头固定、斑马纹、时间列、列宽可调、空状态占位。

**偏好管理页**
- 偏好总结卡片排版（要点列表 + 生成时间），生成按钮 loading 态。

## 四、优先级

1. 整窗布局（品牌栏 + 导航 + 内容区撑满）—— 改观最大、最基础。
2. 配色 Token + 组件（按钮/输入/开关/卡片）统一样式。
3. 服务控制页状态总览卡片。
4. 导航图标 + 选中指示条。
5. 历史表格时间列 + 样式。
6. API 配置分组 + 密码切换。

## 五、可复用「设计预览」

如需先看效果，我可以用 HTML 画一版 mock（配色/组件/布局）给你确认风格，再落到 PySide6。要不要？
